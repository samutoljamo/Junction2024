import { Box } from "@mui/joy";
import { NavLink } from "react-router-dom";

export default function Root() {
  return (
    <Box>
      Hello form map
      <NavLink to="/information">Neeext</NavLink>
    </Box>
  );
}
